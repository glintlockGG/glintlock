{
  "mcpServers": {
    "glintlock-engine": {
      "command": "bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/engine/scripts/start-engine.sh"],
      "env": {
        "GLINTLOCK_WORLD_DIR": "./world",
        "ELEVENLABS_API_KEY": "${ELEVENLABS_API_KEY}"
      }
    }
  }
}
