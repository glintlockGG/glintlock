node_modules/
*.pdf
.DS_Store
.mcpb-cache/
glintlock-plugin.zip

# World state (lives in user's project directory, not in the plugin repo)
world/
world-*/

# Playwright MCP cache
.playwright-mcp/

# Engram project journal (local developer state)
.engram/

# Dispatch artifacts
DISPATCH.md
new-dash.zip
